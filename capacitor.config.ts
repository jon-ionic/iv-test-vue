import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.gettingstartediv.vue',
  appName: 'jon-iv-getting-started-vue',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
